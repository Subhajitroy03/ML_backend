name: Ping Production URL

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit production URL
        run: |
          curl -s https://ml-backend-1-suy7.onrender.com > /dev/null
            echo "Pinged production URL successfully."